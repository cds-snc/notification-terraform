{
    "fileLocations": [
        {
            "URIPrefixes": [
                "s3://${bucket_ca}",
                "s3://${bucket_us}"
            ]
        }
    ],
    "globalUploadSettings": {
        "format": "CSV",
        "delimiter": ",",
        "textqualifier": "'",
        "containsHeader": "true"
    }
}